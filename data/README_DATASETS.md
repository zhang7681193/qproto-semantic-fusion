# Dataset Notes

Experiments use public datasets and generated readout files. Large cached `.npz` files are represented by checksums in `cached_feature_checksums.csv`; scripts document download or generation commands.


